<%if b.reservation_id != nil %>
	<%= link_to edit_bedroom_reservation_path(b,b.reservation_id), class:"update-btn btn-floating right" do%>
		<i class="material-icons">hotel</i>
	<% end %>
<%end%>
<%= link_to edit_bedroom_path(b), class:"btn btn-floating right" do%>
	<i class="material-icons">mode_edit</i>
<%end%>
<%if params[:state_bedroom] != "Desactivado" %>
	<%= link_to new_bedroom_reservation_path(b), class:"reserva-btn btn-floating right" do%>
		<i class="material-icons">event_note</i>
	<%end%>
<%end%>
